# dopatruition

## Status
- **Phase 1 Complete:** Walking Skeleton is live.
- **Current Version:** v0.0.1 (Alpha)

## Quick Start (Linux)
1. **Prerequisites:**
   - Flutter (Manual Install, NOT Snap)
   - Clang/CMake (`sudo apt-get install clang cmake ninja-build pkg-config libgtk-3-dev`)
2. **Setup:**
   - Create a `.env` file in root with `SUPABASE_URL` and `SUPABASE_ANON_KEY`.
3. **Run:**
   ```bash
   flutter pub get
   flutter run -d linux